name: Flatpak Build
on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'docs/**'
      - 'assets/**'
      - 'scripts/**'
      - '**.md'
      - 'mkdocs.yml'
  pull_request:
    paths-ignore:
      - 'docs/**'
      - 'assets/**'
      - 'scripts/**'
      - '**.md'
      - 'mkdocs.yml'
jobs:
  flatpak:
    name: "Flatpak"
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:gnome-49
      options: --privileged
    steps:
      - uses: actions/checkout@v4
      - name: Lint AppStream metadata and manifests
        run: |
          flatpak-builder-lint appstream data/io.speedofsound.SpeedOfSound.metainfo.xml.in
          flatpak-builder-lint manifest io.speedofsound.SpeedOfSound.yml
      - uses: flatpak/flatpak-github-actions/flatpak-builder@v6
        with:
          manifest-path: io.speedofsound.SpeedOfSound.yml
          bundle: speedofsound.flatpak
          cache-key: flatpak-builder-${{ github.sha }}
